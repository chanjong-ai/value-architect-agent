# Layout Blueprint

> `python scripts/deck_cli.py predeck <client> --pages 30 --update-spec` 실행 시 자동 생성/갱신됩니다.

## Usage
- 목적: 리서치 이후 슬라이드별 레이아웃, 본문 밀도, 시각화 지시를 먼저 고정
- 출력 원본: `layout_blueprint.yaml`
- 반영 파일: `deck_spec.yaml`, `layout_preferences.research.yaml`
