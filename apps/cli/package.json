{
  "name": "@consulting-ppt/cli",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "bin": {
    "consulting-ppt-agent": "dist/main.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@consulting-ppt/making": "workspace:*",
    "@consulting-ppt/memory": "workspace:*",
    "@consulting-ppt/qa": "workspace:*",
    "@consulting-ppt/shared": "workspace:*",
    "@consulting-ppt/thinking": "workspace:*",
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "rimraf": "^6.0.1"
  }
}
